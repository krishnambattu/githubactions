name:  run jon every 5min
on:
 schedule:
    - cron:  '*/5 * * * *' # for every 5min #    
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Hello ${{ github.event.inputs.name }}!"
          echo "- in ${{ github.event.inputs.home }}!"
